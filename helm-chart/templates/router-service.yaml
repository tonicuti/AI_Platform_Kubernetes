apiVersion: v1
kind: Service
metadata:
  name: router-service
spec:
  selector:
    app: router # This links the Service to the Deployment above
  ports:
    - protocol: TCP
      port: {{ .Values.router.port }}
      targetPort: {{ .Values.router.port }}